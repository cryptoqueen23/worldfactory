const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('.site-nav');menuButton?.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));nav?.classList.toggle('open',!open)});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuButton?.setAttribute('aria-expanded','false')}));const video=document.querySelector('#promo-video');const toggle=document.querySelector('#video-toggle');const frame=document.querySelector('.video-frame');const reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;if(video&&toggle&&frame){video.addEventListener('loadeddata',()=>frame.classList.add('has-video'));const sync=()=>{toggle.textContent=video.paused?'Play film':'Pause film';toggle.setAttribute('aria-label',video.paused?'Play promotional video':'Pause promotional video')};toggle.addEventListener('click',async()=>{if(video.paused){try{await video.play()}catch{}}else video.pause();sync()});video.addEventListener('play',sync);video.addEventListener('pause',sync);if(!reduced){video.play().catch(()=>{});}sync();}